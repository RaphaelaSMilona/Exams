std::string gene= "ATG";#include <stdio.h>
#include <math.h>
#include <string.h>

int main()
{
        //the average prokaryote gene length is typically around 500-3000bp 
	char seq[3000];	
	while( scanf("%s",seq) ==1 )
	{
        	int lengthseq;
                lengthseq=strlen(seq);
              	int i;
			for(i=0; i<=lengthseq - 2; i++)
			{
					if (seq[i] == 'A' & seq[i + 1] == 'T'  & seq[i + 2] == 'G')
					{
						std::string gene= "ATG";
						
					}
					 if (seq[i] == 'T' & seq[i + 1] == 'A'  & seq[i + 2] == 'G' || seq[i + 2] == 'A')
					{
						std::string stopcodon= "A,";
					}
			}
		i++;
		
	}



}
